{% extends 'base.html.twig' %}
{% block title %}{{ actor.name }}{% endblock %}



{% block body %}
    <h1>{{ actor.name }}</h1>

    <ul>
        {% for program in actor.programs %}
            {#   <li>{{ program.title }}</li>#}
<li><a href="{{ path('wild_show', {'id': program.id}) }}">{{ program.title }}</a></li>

{% endfor %}
</ul>
{% endblock %}
